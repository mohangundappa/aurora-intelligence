# ADR 0006: Configured decision policy

Priority-ordered YAML rules combine signal values, confidence, freshness,
structured attributes, consent, and safe defaults. Reason codes come from the
matched rule and every decision stores policy version and correlation ID.
