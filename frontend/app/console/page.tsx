'use client';
import {useState} from 'react';
const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8080';
export default function Console(){const [session,setSession]=useState('');const [data,setData]=useState<any>();async function load(){setData(await fetch(`${API}/api/console/sessions/${session}`).then(r=>r.json()))}return <main className="console"><div className="shell"><nav className="nav"><div className="brand">AURORA / INTELLIGENCE</div><a href="/" style={{color:'#edf4f6'}}>Aurora site ↗</a></nav><header className="hero"><div className="eyebrow">Customer intelligence console</div><h1>See the why behind every decision.</h1><p className="muted">The local CDP simulator is a stand-in alongside a CDP platform. This view makes derived intelligence explainable.</p><div className="search"><label htmlFor="session" className="sr-only">Session ID</label><input id="session" value={session} onChange={e=>setSession(e.target.value)} placeholder="Paste a session ID" /><button className="button" onClick={load}>Load journey</button></div></header>{data&&<div className="grid"><section className="card"><span className="pill">RAW EVENTS</span><h2>Observed behavior</h2>{data.events.map((e:any)=><p key={e.eventId}><b>{e.eventName}</b><br/><span className="muted">{JSON.stringify(e.payload)}</span></p>)}</section><section className="card"><span className="pill">DERIVED SIGNAL</span><h2>{data.context.signal?.name||'No signal'}</h2>{data.context.signal&&<><p>Value <b>{data.context.signal.value}</b> · Confidence <b>{data.context.signal.confidence}</b></p><p className="muted">Why: {data.context.signal.explanation}</p><p className="muted">Freshness: {data.context.signal.freshness}</p></>}</section><section className="card"><span className="pill">NEXT BEST ACTION</span><h2>{data.decision.experience}</h2><p className="muted">{data.decision.explanation}</p><p>Reason codes: {data.decision.reasonCodes.join(' · ')}</p></section></div>}</div></main>}
